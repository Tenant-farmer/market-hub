# KRX Data Marketplace 무료 계정 (pykrx 필수, 2025-12-27부터 로그인 의무화)
KRX_ID=
KRX_PW=

# SEC EDGAR: 이름/이메일 형식 User-Agent 필수
EDGAR_USER_AGENT=

# 텔레그램 브리핑 (@BotFather 토큰 → 봇에게 메시지 → python -m src.jobs.briefing --setup)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- 이후 단계 ---
# 키움 REST API (모의: mockapi.kiwoom.com)
KIWOOM_APP_KEY=
KIWOOM_APP_SECRET=
KIWOOM_MOCK=1

# Alpaca 페이퍼
ALPACA_API_KEY=
ALPACA_API_SECRET=

# TV 웹훅 페이로드 검증용 시크릿
WEBHOOK_SECRET=

# --- 뉴스/공시/거시 확장 (키 넣으면 수집기 활성 — 없으면 해당 소스만 건너뜀) ---
# DART 공시 (무료 키: https://opendart.fss.or.kr/ → 인증키 신청) — KR 보유종목 실적·자사주·증자 공시
DART_API_KEY=
# 한국은행 ECOS (무료 키: https://ecos.bok.or.kr/api/#/AuthKeyApply) — 기준금리·국고채 수익률
ECOS_API_KEY=
# KRX Open API (https://openapi.krx.co.kr — '파생상품지수 시세정보' 이용신청 후 마이페이지 인증키) — VKOSPI
KRX_OPENAPI_KEY=
# 네이버 뉴스 검색 (선택, https://developers.naver.com — Google RSS 대비 KR 뉴스 업그레이드용)
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# --- 대시보드 접근/바인드 (로컬은 비워두면 무인증·localhost) ---
# VPS 외부 노출 시: DASH_HOST=0.0.0.0, DASH_PASS=강한비밀번호 (HTTPS 터널 경유 권장)
DASH_HOST=
DASH_USER=admin
DASH_PASS=

# --- 자동매매 청산/리스크 (비워두면 기본값) ---
# EXIT_ENABLED=1 이면 워커가 보유 포지션에 청산규칙 적용(손절/주도이탈) → SELL 신호
EXIT_ENABLED=
# EXIT_STOP_PCT=-8   EXIT_RS=0   EXIT_CHECK_SEC=60 (손절 감지 주기 — 매도는 지연이 돈)
# KIWOOM_SELL_TIMEOUT_SEC=120 (키움 매도 미체결 워치독 — 초과 시 취소 후 재제출 + 텔레그램 경보)
# 추세이탈(20MA)은 백테스트상 휩쏘로 해로워 기본 off — 굳이 켜려면: EXIT_MA_ENABLED=1  EXIT_MA=20
# MAX_ORDER_USD=10000   MAX_ORDER_KRW=10000000   MAX_DAILY_ORDERS=20   KILL_SWITCH=
# MAX_DAILY_LOSS_PCT=2  (일손실 한도 %: 전일 에쿼티 대비 초과 손실 시 신규 매수 차단, 0=off)
# --- 신호진입: 매수신호등(green)일 때 지수 자동매수 (기본 off) ---
# SIGNAL_ENTRY_ENABLED=1   SIGNAL_ENTRY_SYMBOL=SPY   SIGNAL_ENTRY_QTY=1   SIGNAL_ENTRY_CHECK_SEC=3600
# SIGNAL_ENTRY_SYMBOL_KR=069500 (KODEX200 — green 시 KR 장중에만 emit, 비우면 KR 진입 없음)  SIGNAL_ENTRY_QTY_KR=1
# --- 주도주 로테이션: 126일 순위 top10 진입/top30 이탈, 주1회, Alpaca 페이퍼 (기본 off) ---
# ROTATION_ENABLED=1   ROTATION_SLOT_USD=1000   ROTATION_SLOT_KRW=2000000   ROTATION_CHECK_SEC=3600
# (KR 로테이션은 백테스트상 실패 전략 — 모의 A/B 관찰용. 정수 주, 장중에만 발주)
# RECONCILE_SEC=300  (주문 체결상태 동기화 주기 — 항상 동작, 안전)
# EVENT_ALERT_SEC=300  (지표·실적 발표 알림 감지 주기 — major 지표 actual 확인 + 감시종목 실적 시간대, 0=off)
